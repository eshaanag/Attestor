id: "7.1.5"
title: "Ensure permissions on /etc/cron.monthly are configured"
benchmark: "CIS Ubuntu Linux 22.04 LTS Benchmark"
benchmark_version: "v2.0.0"
level: 1
profile:
  - server
  - workstation
automated: true
severity: medium
checks:
  - type: file_permission
    path: /etc/cron.monthly
    expected: "0700"
    owner: root
    group: root
    op: equals
remediation: >
  Run: chmod 700 /etc/cron.monthly && chown root:root /etc/cron.monthly
source: >
  Control ID 7.1.5, cross-referenced via reference/UBUNTU22-CIS-Audit (CIS Ubuntu 22.04 LTS Benchmark v2.0.0). VM-verified on Ubuntu 22.04 (192.168.64.6) 2026-08-15.
