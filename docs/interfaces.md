# Interfaces

JSON interface contracts between components (engine output → report generator, engine output → ledger, backend API shapes). Defined here before implementation so both sides build to the same contract.
