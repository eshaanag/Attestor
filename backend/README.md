# Backend

FastAPI fleet collector (Phase 8 stretch goal). Receives audit reports from
multiple hosts and stores them centrally. Only started after Phases 0–7 are
complete and the backend API contract is defined.
