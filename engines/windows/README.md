# Windows Audit Engine

PowerShell audit engine (`run_audit.ps1`) that dispatches CIS control checks against Windows 11 systems. Supports check_types: registry, secpol, audit_policy, service_state, account_policy.
