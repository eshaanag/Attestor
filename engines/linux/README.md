# Linux Audit Engine

Python audit engine (`run_audit.py`) that dispatches CIS control checks against Linux systems. Supports check_types: kernel_module, sysctl, file_permission, service_state, package_installed, config_grep.
