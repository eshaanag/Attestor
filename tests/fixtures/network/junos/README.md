# Junos source-derived corpus

These files are redacted excerpts, not fabricated configurations. Each retained
statement is copied from the cited upstream file at the pinned commit; secret,
key, address, and unrelated topology statements are omitted rather than
replaced. The excerpts preserve the service/security statements needed for the
Junos evidence gate.

Sources and original file hashes are recorded in `manifest.json`.
